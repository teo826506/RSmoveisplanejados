{
  "buildCommand": "DATABASE_URL=\"postgresql://postgres:postgres@localhost:5432/postgres\" npx prisma generate && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
